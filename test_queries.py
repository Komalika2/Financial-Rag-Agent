# test_queries.py
# This file would typically define a list of test queries to run against your agent.
# For this project, the main test queries are currently defined directly in main.py for simplicity.

# Example structure if you were to use it:
TEST_QUERIES = [
    "What was NVIDIA's total revenue in fiscal year 2024?",
    "How much did Microsoft's cloud revenue grow from 2022 to 2023?",
    # Add more queries here
]

if __name__ == "__main__":
    print("This file defines test queries. They are currently used in main.py.")
    print(f"Number of test queries defined: {len(TEST_QUERIES)}")